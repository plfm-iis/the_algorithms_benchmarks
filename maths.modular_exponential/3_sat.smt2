(set-logic ALL)
(declare-const base_VAR Int)
(declare-const power_VAR Int)
(declare-const mod_VAR Int)
(assert (not (< power_VAR 0)))
(assert (not (= mod_VAR 0)))
(assert (> power_VAR 0))
(check-sat)
(get-value (base_VAR))
(get-value (power_VAR))
(get-value (mod_VAR))
